<%= form_with(model: stat, local: true) do |form| %>
  <% if stat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stat.errors.count, "error") %> prohibited this stat from being saved:</h2>

      <ul>
      <% stat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :stat_name %>
  </div>

  <div class="field">
    <%= form.label :elem %>
    <%= form.number_field :elem, id: :stat_elem %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.number_field :total, id: :stat_total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
