<%= form_with(model: album_stream, local: true) do |form| %>
  <% if album_stream.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(album_stream.errors.count, "error") %> prohibited this album_stream from being saved:</h2>

      <ul>
      <% album_stream.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :album_id %>
    <%= form.text_field :album_id, id: :album_stream_album_id %>
  </div>

  <div class="field">
    <%= form.label :stream_id %>
    <%= form.text_field :stream_id, id: :album_stream_stream_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
