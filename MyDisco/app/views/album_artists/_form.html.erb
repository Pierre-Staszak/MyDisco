<%= form_with(model: album_artist, local: true) do |form| %>
  <% if album_artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(album_artist.errors.count, "error") %> prohibited this album_artist from being saved:</h2>

      <ul>
      <% album_artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :album_id %>
    <%= form.text_field :album_id, id: :album_artist_album_id %>
  </div>

  <div class="field">
    <%= form.label :artist_id %>
    <%= form.text_field :artist_id, id: :album_artist_artist_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
